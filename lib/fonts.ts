import { Inter } from "next/font/google";

export const inter = Inter({
  weight: ["400", "500", "700"],
  display: "swap",
  subsets: ["latin"],
  variable: "--font-inter",
  fallback: ["ui-sans-serif"],
});
